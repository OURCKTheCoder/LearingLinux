#include<stdio.h>
#include<string.h>
// Stdlib for file operations. Based on FILE (or so called stream).
// This program includes:
/*	1. FILE *fopen(const char *filename, const char *mode)
 *	2. size_t fread(void *ptr, size_t size, size_t n_items, FILE *stream)
 *	3. size_t fwrite(const void *ptr, size_t size, size_t n_items, FILE *stream)
 *	4. int fclose(FILE *stream)
 *	5. int fflush(FILE *stream)
 *	6. int fseek(FILE *stream, long int offset, int whence)
 */
int main()
{
	// 1. Open & create a file.
	
	// 2. Write some cool shit in it.
	
	// Reset the fd ptr.
	
	// 3. Read cool shit out.
	
	// 4. Close file.
	
	return 0;
}
